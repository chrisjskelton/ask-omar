# Ask Omar Omarchy plugin

This directory is the user-owned Omarchy bar widget. It communicates with the
local `ask-omar` service and contains no provider credentials.

The bar stays visible while a focused layer-shell surface is mapped over the
field during text entry. This is necessary because the Omarchy bar itself is
intentionally configured with no keyboard focus.
